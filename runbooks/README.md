# Runbooks

Store repeatable operating procedures for this workstation here.

Every runbook should include prerequisites, exact commands, expected output, validation, rollback, and whether administrator privileges or a reboot are required. Never embed credentials or hardware serial numbers.
